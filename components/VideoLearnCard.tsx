import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const VideoLearnCard = (props: Props) => {
  return (
    <View>
      <Text>VideoLearnCard</Text>
    </View>
  );
};

export default VideoLearnCard;

const styles = StyleSheet.create({});
